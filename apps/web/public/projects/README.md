Drop the two Pollio screenshots here, named exactly:

  pollio-landing.png   — the marketing page (Get instant feedback from your audience)
  pollio-app.png       — the logged-in Home / presentations dashboard

The projects page checks whether each file exists and only renders it if it does,
so nothing breaks while they are missing.
