Drop the hero image here, named `hero` with any common extension:

  hero.png   hero.jpg   hero.jpeg   hero.webp

The hero checks for these in order and renders the first one it finds, reading
the real dimensions from the file. Nothing breaks while it is missing.
